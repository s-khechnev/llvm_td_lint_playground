(rule
 (targets ADD.json CPOP.json CPOPW.json ADDI.json ANDI.json ADD_UW.json)
 (deps ../RISCV.instructions.json)
 (mode
  (promote (until-clean)))
 (action
  (run ../dissect.exe %{deps} -split-by-keys)))
